S7.1 — the SEO gate goes red, proven eight ways
=================================================================
Generated by breaking the built output one way at a time and running
"npm run seo" against each break, then restoring. The last block is the
restored tree, green. Nothing below is hand-written: it is the gate's own
stdout/stderr and exit code.

The point of the file is that every one of these failures is invisible on
the rendered page. Each block is a state a browser, "npm run check" and
"npm run build" are all perfectly happy with.

### a page's canonical points at another URL
exit 1
seo: 2 problem(s).
  /concepts/agent-memory/: canonical says this page lives at "/concepts/agent-observability/", but it is served at "/concepts/agent-memory/". A crawler believes the canonical, so this page would be dropped in favour of a URL that may not exist.
  /concepts/agent-memory/: og:url is ["https://conductor-site-virid.vercel.app/concepts/agent-memory/"], which is not the canonical https://conductor-site-virid.vercel.app/concepts/agent-observability/.

### a canonical on the wrong origin
exit 1
seo: 1 problem(s).
  /: canonical is "https://conductor-site.vercel.app/", which is not on the site origin https://conductor-site-virid.vercel.app.

### a page dropped out of the sitemap
exit 1
seo: 1 problem(s).
  sitemap.xml is missing "/articles/the-ledger-that-lied/". A page not in the sitemap is a page not crawled.

### robots.txt stops disallowing the card renderer
exit 1
seo: 1 problem(s).
  robots.txt disallows [/api/, /edit]; the built paths that are not content are [/api/, /edit, /og/]. Change src/pages/robots.txt.ts, or NOT_CRAWLED in this file if the site really did grow a new non-content prefix.

### robots.txt Sitemap: line left on the old domain
exit 1
seo: 1 problem(s).
  robots.txt points at "https://conductor-site.vercel.app/sitemap.xml" but the site is https://conductor-site-virid.vercel.app. A Sitemap: line on the previous domain is the quietest way to publish a sitemap nobody reads.

### an og:image PNG that is not 1200x630
exit 1
seo: 1 problem(s).
  /og/runs.png: 1200×640. The cards are 1200×630 — re-take the screenshot with the viewport at that size (docs/OG-CARDS.md).

### an og:image naming a file that was never written
exit 1
seo: 5 problem(s).
  /articles/: og:image names "/og/articles.png", which is not in dist. This is the grey box in a feed, and nothing on the page shows it.
  /articles/never-believe-the-agent/: og:image names "/og/articles.png", which is not in dist. This is the grey box in a feed, and nothing on the page shows it.
  /articles/the-ledger-that-lied/: og:image names "/og/articles.png", which is not in dist. This is the grey box in a feed, and nothing on the page shows it.
  /articles/the-nudge-below-the-median/: og:image names "/og/articles.png", which is not in dist. This is the grey box in a feed, and nothing on the page shows it.
  /articles/what-a-run-costs/: og:image names "/og/articles.png", which is not in dist. This is the grey box in a feed, and nothing on the page shows it.

### the corpus moved under a card that was already taken
exit 1
seo: 1 problem(s).
  og/home is stale. The PNG in public/og was taken from a corpus harvested 2026-08-07T02:13:47.458Z, and the card renders differently now:
    was: field guide conductor-site-virid.vercel.app A field guide to agentic engineering Ten concepts, each worked end to end in a real orchestrator, with what it cost. 18 runs 281/300 checkpoints closed $3,016.29 spent across the corpus
    now: field guide conductor-site-virid.vercel.app A field guide to agentic engineering Ten concepts, each worked end to end in a real orchestrator, with what it cost. 18 runs 287/300 checkpoints closed $3,016.29 spent across the corpus
    The picture in circulation still shows the old figures. Re-take the four screenshots — docs/OG-CARDS.md.

### restored
exit 0
seo: https://conductor-site-virid.vercel.app — 22 public page(s) all carry their own canonical, og:url and og:image; the sitemap lists exactly the 21 crawlable one(s); robots.txt disallows only [/api/, /edit, /og/]; and 4 social card(s) at 1200×630 still render the text their PNGs were taken from.
